

const GameTitleCard = (props) => {
    return (
        <div className="card">
            <h5 className='card-title text-dark'>{props.cardTitle}</h5>
        </div>
    )
}

export default GameTitleCard;